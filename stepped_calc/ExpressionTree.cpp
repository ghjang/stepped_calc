#include "ExpressionTree.h"


ExpressionTreePtr
make_expression_tree(token_list_t const& postfixNotationTokens)
{
    ExpressionTreePtr ptrTree;
    return ptrTree;
}

operand_t
evaluate_expression_tree(ExpressionTreePtr ptrTree)
{
    assert(ptrTree);
    if (!ptrTree) {

    }

    operand_t result;
    return result;
}

